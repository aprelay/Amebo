# This file enables GitHub Actions auto-deploy
# Setup instructions in AUTO_DEPLOY_SETUP.md
#
# NOTE: This workflow file needs to be added manually to GitHub
# because Personal Access Tokens require 'workflow' scope to create workflows.
#
# To enable auto-deploy:
# 1. Copy this entire file
# 2. Go to: https://github.com/aprelay/Amebo
# 3. Click "Add file" → "Create new file"
# 4. Name it: .github/workflows/deploy.yml
# 5. Paste this content
# 6. Commit directly to main branch
# 7. Add required secrets (see AUTO_DEPLOY_SETUP.md)

name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      deployments: write
    
    name: Deploy to Cloudflare Pages
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build project
        run: npm run build
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'
      
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: amebo-app
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
